# Known issues and limitations

Last updated: 2026-09-22

| ID | Status | Issue | Consequence / next action |
|---|---|---|---|
| KI-001 | Open | `Veldwake` is only a working title; preliminary transcript research is not legal clearance. | Perform formal INPI/USPTO/EUIPO, store, domain, and counsel review before public branding. |
| KI-002 | Open | The only executable is a Windows-first diagnostic renderer, not a playable game; other OSes/backends are unverified. | Expand support only when a milestone has a concrete target and validation host. |
| KI-003 | Open | Integrated GPU and shared memory make the audit host useful as a conservative dev target, not a complete hardware matrix. | Add discrete-GPU and lower-end CI/manual test coverage later. |
| KI-004 | Open | Vulkan loader reports an OBS hook layer older than the requested Vulkan API and a missing layer-manifest registry lookup. | D3D12 remains the primary Windows path; reassess only if Vulkan testing becomes required. |
| KI-005 | Open | No project license is selected. | Owner decision required before public distribution/contributions. |
| KI-006 | Open | M1 through M4 have one-host visual validation but no automated visual regression or multi-adapter coverage. M4 adds named camera poses and locked content fixtures, which make two captures comparable, but nothing compares them automatically and no second adapter has been tested. | Add stable capture fixtures and broader hardware coverage when rendered game content begins to change. M4's `region::GOLDEN_POSES` is the anchor a future comparison would use; a pixel comparison is deliberately not the oracle, because a driver change would break it without a voxel moving. |
| KI-007 | Accepted | `cargo-deny` reports transitive duplicate pairs for `hashbrown` and `syn` in the current `wgpu` graph. | Keep duplicate visibility as a warning; reassess on dependency upgrades rather than forcing transitive versions. |
| KI-008 | Open | The audited Windows host has intermittently produced linker `LNK1104` for test executables when `cargo nextest` follows another Cargo gate, even sequentially. Recurred twice on 2026-09-16 during M3B work and twice consecutively during M3C0 (no holder process, files absent); the third plain run and a `--build-jobs 1` run both passed 86/86. During M3C1 it failed four consecutive times (including `--build-jobs 1`) before passing; a bounded retry loop of five plain runs is now the working procedure. | Verify no project/Cargo process is holding the named executable, then rerun `cargo nextest` alone (a bounded loop of up to five plain runs, stopping at the first run without `LNK1104`). This retry applies only to the linker/host lock: an assertion failure, panic, failing test, Clippy or build error, or any other gate result is evidence and must never be re-run until it passes. Do not clean artifacts or change code unless the isolated retry also fails. |
| KI-009 | Accepted | (a) Presentation-only invalidations — a level change of the chunk or a neighbor, render-membership churn — used to stop an already drawn mesh until its replacement was meshed and uploaded; on the M3C2 path this opened 909 gaps of up to 232 frames with 62 chunks missing at once. (b) Data invalidations still stop stale geometry immediately. | (a) Resolved by retaining the committed mesh until a seam-coherent group commit: current QA recorded zero gaps where previously drawn meshes disappeared and zero mixed-seam invariant failures. This does not cover never-drawn frontier entrants; KI-014 tracks those separately. (b) Accepted by design: never draw data that is gone/unproven or turn an unavailable neighbor into AIR. |
| KI-011 | Open | Transition groups are connected sets of changing seams and are unbounded (78 chunks observed at the corridor start); while the camera keeps crossing the band the `Lod0` ring's group is re-dirtied before it commits, so drawn chunks can keep a coherent but stale level until the camera slows. | Never a hole or a mixed seam; converges at rest (`transition_pending = 0`). Now directly observable: the M3C3 `Boundaries` view draws every uncommitted group member in violet and switches to red at 16 members, and the driven smoke shows large groups persisting across a traversal. Measure `blocked_group_max` and `constrained_undrawn_*` in the swap-churn investigation; bound groups only with a proof that a partial commit cannot expose a mixed seam. |
| KI-012 | Accepted | The debug views allocate one uniform buffer, one bind group, and one draw call per primitive. Measured on the audited host with an idle camera: 637 residency boxes take the client from 60 FPS to 37.6 and then 15.4 FPS (26.6 ms, then 64.8 ms per frame); 180 boundary primitives give 46.7–47.9 FPS. | Deliberate diagnostic pattern. In `Off`, current instrumentation reports zero per-frame debug primitive allocations, uniform writes, and draws, with mesh upload budget unchanged. Fixed startup pipeline/unit geometry and slots retained after prior use still consume resources; “off” never meant zero fixed residency. Do not add batching/render graph without a milestone need. |
| KI-013 | Open | LOD's chunk-mesh memory benefit against the **diagnostic fixture** is much smaller than M3C2 reported. Mutation-boundary sampling (including after stage and before commit) gives a peak simultaneous presentation-owned chunk-mesh total of 12,101,888 → 10,053,696 (−16.9%), while upload bytes rise 81.6%. The banded peak is run-dependent: five later runs of the identical path measured 9,734,816 to 9,800,128 (−19.0% to −19.6%), so the saving is a range and a single sample is not reproducible. This is not global GPU memory. | Earlier −57%/−59% counted committed buffers only; the later −19% figure still sampled only at update end and missed intra-update overlap. LOD stays opt-in. Future claims must use mutation-boundary simultaneous totals and separately account for depth, pipelines, debug resources, and driver/wgpu retention when global GPU memory matters. M4 measured a much larger saving against real terrain — 21,806,552 against 65,992,424 settled chunk-mesh bytes at the same camera, −67% — but that is settled committed bytes at one pose, not a simultaneous peak over a traversal, and the two numbers must not be compared as if they measured the same thing. |
| KI-014 | Open | A zero “previously drawn gap” count does not prove complete frontier coverage. On the QA banded path, up to two non-empty CPU-ready chunks were staged but temporarily withheld by their seam-coherent transition group (15 updates, 21 chunk-frames); historical debug smoke reached 13 but lacked cause attribution. | Treat non-zero `ready_undrawn` as a real temporary coverage hole, even when seam-safe and bounded. Preserve the split metrics (`frontier_pipeline_pending`, `ready_awaiting_upload`, `ready_blocked_transition`, `committed_missing`). Investigate alongside KI-011; never claim “zero holes” from `gaps_closed = 0` or a few visually continuous captures. |
| KI-015 | Open | The M3D disk cache has no eviction or size policy. A changed format/source identity uses another directory, leaves earlier identities on disk, and nothing bounds total growth. Cold `ChunkCache::open` walks the current identity synchronously, so a very large opt-in cache can also lengthen startup. | Deliberate for an experiment: footprint is reported explicitly and `ChunkCache::clear` removes one identity's entries. Production needs size/age policy, sharding/region layout, and a decision about asynchronous startup against real world generation. No directory scan occurs per frame. |
| KI-017 | Open | Settling the M4 golden profile takes about a minute (59.7 s on the audited host for 3,211 loads and 351 meshes). The runtime dispatches at most one worker job per `poll` and `poll` runs once per frame, so at 60 Hz throughput is bounded at roughly sixty jobs per second whatever the worker can do. Most of those jobs resolve to authoritative absence: the demand set is a cube 13 chunks tall while the region is 3. | Not a rendering cost and not a correctness problem: every capture settles, and the counters stay clean. Two independent avenues if it ever matters — dispatch more than one job per poll, and stop spending a load on coordinates a source could reject from its extent alone. Neither belongs in M4; both need their own evidence. |
| KI-016 | Open | On the **diagnostic fixture** the cache is slower than regeneration. The original three-run set measured warm at 13–37 ms versus 6–7 ms cacheless; after QA removed an accidental maximum-size reservation per read, the final sample measured raw warm at 15–17 ms versus 5 ms and RLE warm at about 8 ms versus 4 ms. The source is trivial and timings are noisy, but no run demonstrates a benefit. | Scoped to the diagnostic source by M4 evidence. Against real terrain generation the cache is clearly worth having: a warm run of the same 81 chunks reaches idle in 11 ms where regeneration takes 68. The entry remains open because the cache is still opt-in and still an experiment, and because the diagnostic finding stands as written for that source. |
| KI-018 | Open | A planted foot is not pinned to the world; its position is computed from the gait each frame rather than remembered. On level ground that is now nearly exact — a planted ankle wanders `0.040` world units walking and `0.055` running, under half and two thirds of one character voxel — because stride, duty factor and pelvis drop are derived together and the stance sweep is linear. On a terrain terrace it is not: when a stance crosses a step the sole follows the new block over the next few frames instead of staying where it was put. On a `0.18` stepped ramp over 1,200 simulated steps the worst planted-sole deviation stays under one terrain voxel and the solver reports `reached == false` for every frame it could not satisfy. | Bounded by two tests: `a_planted_foot_does_not_slide_on_level_ground` measures the level case and requires under one character voxel, and `a_terrain_scale_terrace_is_bounded_and_reported` bounds the terraced one. The remaining cure is standard and named: remember a planted foot's world position for the length of its stance and drive the pelvis from the pinned feet, which also removes the last of the level-ground residue. That is a locomotion change with its own evidence and does not belong in a milestone whose contact claim is "the sole sits on the block the viewer can see". |
| KI-019 | Accepted | The character casts no shadow on itself. The character shadow pass culls front faces, like the chunk pass, because a `2048`-texel map over a `112`-unit radius is `0.109` world units per texel — larger than one character voxel — and back-face culling produced stripes of acne across the chest in every close capture. | Accepted: self-shadowing at this map resolution was never legible, and the acne was. The character still receives terrain shadows and casts onto terrain. Revisit only with a higher-resolution or character-local shadow cascade, which is a rendering milestone rather than a character one. |
| KI-020 | Open | A rigid limb's end cap emerges through its child at a flexed joint. Closing a joint means overlapping the child part with its parent, and when the joint bends, the parent's cap — a downward-facing face, so lit only by ambient — appears along the intersection as a dark line about a tenth of a voxel wide at a six-degree flex, growing with the angle. Verified not to be z-fighting: captures with and without a per-part depth tie-break are identical. | Accepted consequence of rigid parts ([ADR-0004](adr/0004-rigid-voxel-character-and-analytical-locomotion.md)), sub-pixel at portrait distance and visible only magnified. Any real fix changes what a limb is — a rounded or tapered joint, or a child wide enough to swallow its parent's cap — and belongs with whatever milestone first needs joints to read at close range. |
| KI-021 | Open | Character validation is one-host and one-adapter like everything before it, and no capture is compared automatically. M5 adds locked structural fingerprints and a behavioural signature, so a changed voxel, bone or gait fails a test without a GPU, but nothing checks that the rendered result still looks right. | Same action as KI-006, of which this is the character-domain instance. The named character camera poses and the two stand points are the anchors a future comparison would use. |
| KI-022 | Open | Verified visually by branch QA against the drawn volumes: clear torso contact hits, a clear gap misses, and a blade above the head misses, with no frame found where a blade crosses a core without a hit or deals damage from the air. The combat hurt volume is the torso column only, so a cut that reaches an outstretched arm, a trailing shin, a foot or a weapon does not register. It is one upright capsule per body, refitted to the current pose each tick, and every limb is outside it by construction. | Accepted and measured rather than assumed: a single upright capsule that holds a leg at the top of its stride is `0.83` wide on a body `0.50` through the chest, which is M5's whole-body capsule again and takes hits in empty air. The attack's active window sweeps a height band of `1.14` to `2.84` above the ground, which is chest and head, so nothing M6's two attacks can do is lost by it. A second volume for the limbs is the fix, and it belongs with whatever milestone first has an attack that aims at one. |
| KI-023 | Closed | A successful dodge was thought to read as a crouch rather than as distance. | Closed 2026-09-19 by branch QA, which found the premise wrong rather than the dodge. The `successful-dodge` moment predicate only asked whether the player was dodging while a blade happened to be live, so the captures taken at it were of a dodge that **failed** — the player is staggered ten ticks after the frame. With the predicate corrected to require the swing to run out its whole active window without touching the dodger, the moment moves to tick `2092` and the capture shows `3.57` world units between the bodies, the blade swinging through empty air, and the player's health unchanged across `+0`, `+12` and `+30`. It reads as a spatial evasion and no tuning was needed. |
| KI-024 | Closed | No played run had produced a victory. | Closed 2026-09-19. Branch QA played the encounter in an observe-decide-act loop and recorded **two victories and nine defeats**; the victory was captured inside the defeat hold with `adversary_health=0`, `outcome="adversary"` and the player alive on `42`. Getting there required a real fix rather than better play: from positions the aim table says connect, the player landed one swing in four, because facing follows movement and a body that stands still to swing cannot track one that is moving. A swing now turns onto the other body as it commits, inside a `35°` cone and the reach of the attack, and the hit rate went to sixteen of twenty-four. |
| KI-025 | Open | The camera does not solve occlusion. The follow camera clamps to a minimum height above the ground under it and offsets to one side, and that is the whole of it: a tree between the camera and the fight hides the fight, and at contact range the player's own body can cover the adversary. **M8 gave it its first man-made occluder**: branch QA captured the camera inside a gate shaft while the body walked close past it. The body and the way ahead stayed visible and navigation was unaffected. | An explicit non-goal of M6. `F4` detaches the camera to look around, and the named combat poses frame a fight from outside it for captures. Camera collision is a rendering or camera milestone with its own evidence. |
| KI-026 | Open | The follow camera looks into ground that descends behind the body. Walking down to the river put the body on ground at about `15.9` and the camera at `(-73.5, 19.5, 30.8)`, over ground at about `19.0` — `3.58` above the body's ground and only `0.5` above its own. At `FOLLOW_PITCH = -0.24` a sightline from half a unit up meets the ground about two units out, short of a body six units away, so the capture came back with a flat green plane across the lower half of the frame and **no player in it at all**. | Accepted for M7 and an instance of KI-025 rather than a new class: this is terrain between the camera and the subject, and an occlusion solver is explicitly out of M7's scope. A clamp against the ground under the body was written and measured to move the camera `0.05` world units in this case, so it was reverted rather than kept as a fix that fixes nothing. `F4` detaches the camera. The camera position is now in the `combat state` report, which is what made this diagnosable in one run instead of by reasoning backwards from a screenshot. A real fix raises the camera until its sightline clears the near ground, which is a camera milestone with its own evidence. **Branch QA reproduced it with numbers rather than with a screenshot:** on the meadow the camera sat at `y = 22.574`; with the body held at the waterline (`z = 24.818`) it sat at `y = 19.550`, `3.02` lower, while the camera's own `z = 30.949` was still over the higher meadow — so the camera was inside the bank behind the body. It stayed there for the whole twenty-second push. Across a seventeen-capture tour of the region the body was out of frame in three of them, all on ground that falls away behind it. |
| KI-027 | Open | The route start's component holds `307,144` of the region's `622,023` standable columns, and the *larger* component holds `314,861`. Nearly half the ground a body could stand on cannot be walked to from where a session begins, under the accepted step-up and max-drop bounds. There are also 18 columns reachable one way only — a body can drop in and not climb out. | Evidence, not automatically a defect, and recorded rather than engineered around: no jump, climb or swim was added to improve the number. The highland *is* reachable (243,333 columns, nearest 22 steps from the start), so the split is not the valley wall. Whatever milestone first needs the whole region navigable should start from the barrier attribution — water `2,464`, step-up `44,495`, max-drop `1,273` — and decide deliberately between changing the terrain controls, changing the movement bounds, and adding a traversal verb. |
| KI-028 | Open | A body held against water has no visual or audible cue. It stops dead on the axis that would enter the river and slides along the shore; there is no wading, no splash, no shoreline animation and no sound. | The accepted consequence of the owner's M7 decision that water blocks traversal, and the smallest honest implementation of it. Whether it reads as a comprehensible edge or as an invisible wall is one of the questions the M7 owner playtest asks. A cue belongs with whatever milestone first gives water any behaviour at all. |
| KI-029 | Closed | One adversary stands in an 800 x 800 region with no affordance of any kind pointing at it, so free exploration can miss it entirely. The M7 owner playtest demonstrated this directly: the owner explored the region, judged the traversal a pass, and did not find the adversary. **Closed 2026-09-22 by the M8 owner playtest**, which is the same person in the same region under the same rules: told only to play normally, the owner noticed two landmarks, read them as different destinations, chose one, walked to it and found the adversary while exploring. The affordance is world content — a silhouette worth walking to — and still not a minimap, compass, marker or waypoint. | Expected for M7 rather than a defect in it: the milestone proves traversability and deliberately builds no encounter-discovery system — no minimap, compass, quest marker, waypoint, landmark or navigation aid — and none was added in response. The consequence is that the exploration-to-combat transition, a player victory and the session continuing past a victory remain technical and QA evidence, never owner-observed. Whatever milestone first places more than one thing worth finding is where discovery affordances get designed; this entry exists so that decision starts from an observation rather than from an assumption. Distinct from KI-027, which is about region connectivity under the movement rules. |
| KI-030 | Open | An encounter reset moves the streaming anchor the whole length of the region in one tick, and the far field takes about half a minute to catch up. A player defeat teleports the body from the route goal `(14.5, 191.5)` back to the route start `(-68.5, 49.5)`, `164.5` units. Measured on the audited host: `1.4` s after the reset the streaming report showed `load_queued = 1,450` and `mesh_waiting = 859`, and the backlog reached zero `30.2` s later. `render` stayed at `2,197` throughout, so nothing already drawn was lost; what is missing is everything beyond it, which the capture at the reset shows as a grey horizon where the sky belongs, and one frame with the camera inside the terrain while the body arrives. | Accepted for M7 and not engineered around. The body, the rules and the encounter are correct from the first tick after the reset — the player stands exactly `0.000` from its configured start, the adversary exactly `0.000` from its goal, and both are immediately controllable — so this is a presentation recovery time, not a correctness failure. It is the same demand rail KI-017 is about, meeting a discontinuity no walking speed can produce: the rail is sized for a body that walks. Whatever milestone first adds fast travel, a jump between places, or a second encounter somewhere else has to decide deliberately between prefetching around a known teleport, holding the frame until coverage returns, and accepting the horizon. Distinct from KI-017, which is about keeping up with a walk. |
| KI-031 | Open | Every column-shaped claim about the region is exact only at column centres. The generator decides a column from its centre; `TerrainGround` and `TerrainWalkability` both sample the terrain field continuously, wherever they are asked. Branch QA measured the gap from the generated voxels: at column centres the ground query equals the top face of the drawn ground in `5,120` of `5,120` columns, exactly; away from the centre it differs on about a fifth of sampled points, by up to `2` voxels. The water veto disagrees with itself inside a column too, and only ever in a one-column band along the drawn waterline. | Accepted, and now asserted rather than assumed: `the_ground_query_matches_the_generated_voxels_at_every_column_centre`, `away_from_a_column_centre_the_ground_query_may_differ_but_only_within_two_voxels` and `the_water_veto_disagrees_inside_a_column_only_along_the_waterline` pin all three facts, and the first of those would fail if the grid, the audit or the adapters ever drifted apart. This is the reason the reachability audit is an upper bound and not a measurement, and the reason the route is separately re-proved against the runtime adapters at walk-speed resolution by `the_runtime_accepts_every_step_of_the_route_it_walks_continuously`, which refuses none of its `7,000`-odd steps. Nothing was changed: making either adapter column-quantised would move the M5 and M6 locked signatures and is a decision for whichever milestone needs exactness off-centre, not for QA. |
| KI-037 | Open | The impact synth cannot tell the two weapons apart. The client derives a hit voice with `intensity = damage / victim_max_health * 4.0`, clamped to `[0, 1]`; against a `96`-health adversary the original weapon's `24` damage gives exactly `1.000` and the found weapon's `32` gives `1.333`, which clamps to the same `1.000`. `weight` comes from the **victim's** body height rather than from the weapon, so it does not move either, and the whiff voice is a constant `0.75`. Two weapons of visibly different mass therefore land with an identical sound. | Measured rather than assumed, and deliberately not fixed: the M9 product gate is combat choice, not audio variation, and the owner ruled that no audio work belongs in the milestone. Nothing in `VoiceParams`, the intensity mapping, the synth architecture or the whiff was touched. The mapping is already saturated at M6's damage, so this is evidence for whichever milestone first takes impact audio as its subject — that milestone should decide what `intensity` is a fraction *of* before adding a weapon dimension to it. |
| KI-038 | Open | The adversary has no navigation, and M8 gave it a wall to walk into. Approached from the east, it steers straight at the player, meets the spire's keep-out and stops: a driven M9 session recorded `brain="approach"` with the adversary's `x` frozen at `-133.86` and the separation stuck at `8.72` world units for the rest of the run, and a second run reproduced it. The same session succeeded when the player approached from the south, where no stone lies between the bodies. | M6's explicit non-goal — "no navigation. The adversary steers, the arena is a disc, there is no navmesh" — meeting M8's landmark keep-out. Nothing in M9 caused it and nothing in M9 addresses it. It matters now because M9 gives a player a reason to arrive at the spire from a direction M7's derived route never used: the route approaches from the overlook, and a player who visits the gate first arrives from the east. Whatever milestone first wants an adversary that can be fought from any side has to decide between steering around an obstacle, a navmesh, and placing adversaries where no landmark can screen them. |
| KI-039 | Open | The current encounter does not demand the dimensions a weapon sidegrade trades. In the M9 owner playtest the natural strategy the owner arrived at unprompted was to approach the adversary, stand facing it, attack repeatedly and win — and then to do essentially the same thing with the other weapon. Asked to look for the differences deliberately, the owner **did** perceive the reach difference and the timing and motion difference, and still reported that combat stays simple enough for neither to change the strategy: *"sempre só acaba acertando ele de qualquer jeito"*. Reach, commitment, whiff exposure and dodge availability exist technically — all four are measured in `planning/M9_MEANINGFUL_REWARD.md` — and can largely be ignored by a player. **Investigated and exhausted as a tuning problem** (scratch harnesses outside the repository, owner-derived attack-spam policy against five intentional policies with a human perception model, gated on the real golden traversal over three approaches): adversary damage and health, aim assist, stagger length, telegraph length, strike range, a hold band, the brain's `Recover` duration, the adversary's attack recovery, a commit point on the telegraph (K), recovery punishment (P), and threat-aware approach (T) as both T-Hold and T-Reposition. Against every one the attack-spam strategy stayed a reliable six-of-six win in every ordinary golden approach with both weapons. | **This is about decision pressure, not about the combat system** — M6's readability, telegraph, impact and camera gates stand. The surviving conclusion is geometric: the player closes the distance at `3.4` world units per second while attacking, the adversary repositions at `1.8`, and every adversary movement intent is clamped to the shared `3.4` ceiling, so **ordinary movement and state changes cannot deny a pursuing attack-spammer** — they cannot open separation from a player moving at the shared movement cap. Further parameter tuning is rejected as the next step. A future combat milestone, proposed separately from `main`, must test a broader adversary capability for defending against and managing pressure; which one is not prescribed. K and P are findings, not approved rules. Distinct from KI-038, which is about the adversary walking into landmark stone and did not occur on any gated approach. |
| KI-010 | Closed | M3B2 initially lacked visual verification of seams, unloaded-as-AIR, re-entry, and window lifecycle. | Closed 2026-09-16 by the driven Windows/D3D12 smoke recorded in `planning/M3_STREAMING_WORLD.md`: scripted traversal with screenshot inspection, re-entry, resize, minimize/restore, focus loss, and Escape all verified on the audited host. The transient invalidation blink is log-evidenced only, and coverage remains one integrated-GPU host (KI-003, KI-006). |
| KI-032 | Open | The crown of a first-choice landmark stands above the default camera frame. **The M8 owner read the gate as a broken construction rather than as a gate**, which is consistent with its lintel being off-frame from the overlook — two shafts and sky between them is what a fallen frame looks like. That connection is this repository's inference; the owner was not asked and said nothing about why. At 62 units the spire's top is `19.9` degrees above the follow camera and the frame's top edge is `13.75`, so a player at rest sees a tower leaving the top of the screen rather than a complete silhouette, and the gate's lintel — the thing that makes a gate a gate — is off screen from the overlook. | Deliberate and measured against its alternative. Requiring the whole silhouette inside the frame pushes the pair to 87 and 96 units, where the canopy leaves `8.3` and `11.5` visible voxels instead of `21.3` and `22.0`; both compositions were captured before choosing. A player resolves it by looking up or walking closer, and `the_crown_of_a_first_choice_stands_above_the_default_frame` locks the count so a future change reads as a decision. The owner's session passed the discovery gate with this in place, and branch QA reproduced both halves: `19.9` degrees against a frame edge at `13.75`, and a driven capture from the overlook in which the gate reads as two shafts and sky. |
| KI-033 | Open | A diagnostic world under an arbitrary seed may carry no landmarks. `LandmarkControls::golden()` is a composition tuned to one valley; of twenty-five seeds tried by branch QA — twenty consecutive and five edge values including `0` and `u64::MAX` — **fourteen carry the composition and six fail** with a named reason, and **none** failed to resolve an overlook or panicked — no pair of sites visible and far enough apart, or no site hidden from the overlook and visible from a first landmark. Those worlds are built **without** landmarks rather than with one placed where the composition does not hold. | Accepted and asserted: only the golden world is required to carry all three, `a_world_the_composition_does_not_fit_has_an_overlook_and_no_landmarks` pins seed 1 as a witness (branch QA confirmed seeds 1, 2, 4, 15, 16, 17 and 0 are bare and the other nineteen compose), and `TerrainGenerator::with_seed` falls back to `LandmarkPlan::bare`, which still resolves the overlook. A generator that composes landmarks for any seed needs controls derived from the valley rather than named for it, and that is a milestone of its own. |
| KI-034 | Open | The golden route brushes one corner of a landmark's keep-out. A diagonal step passes between a free column and a blocked one, so a body's capsule crosses the keep-out margin of a column it never stands in. Movement legality is destination-only under [ADR-0008](adr/0008-traversal-legality-separate-from-ground-contact.md), and the runtime walks the step without complaint. | Accepted, counted rather than forbidden: `no_step_of_the_route_cuts_a_corner_between_two_blocked_columns` asserts no diagonal has **both** shoulders blocked and asserts the brush count is the one M8 measured, so a second brush is a deliberate change. The keep-out already carries the widest body's radius as margin, so the brush is inside the margin and not inside the stone. A swept test would be a collision engine, which is an explicit non-goal. |
| KI-035 | Open | Building a world now costs a landmark plan: `248`–`409` ms in release and `1.1`–`1.5` s in debug on the audited host across two measurement sessions, paid once per `TerrainGenerator`, eagerly, before any chunk is generated. The test suite pays it once per test process — `cargo nextest` runs one process per test — which took `veldwake-procedural` from 60 s to 94 s and `veldwake-client` from 114 s to 142 s on this host. | Deliberate: a lazy plan would hide a fallible, expensive derivation behind the first chunk. The reuse that helps is explicit (`WorldSelection::build`, `TerrainGenerator::with_plan`) and the derivation was already taken from `3.2 s` to `248 ms`. If it ever matters again, the remaining levers are the candidate lattice, the level-and-dry disc and the number of tests that build a whole world — not a cache. |
| KI-036 | Open | The landmark family still reads somewhat close to the visual language of Cube World and familiar voxel-RPG architecture. The owner's words after the M8 playtest, given alongside a pass: the structures are simple, but good and sufficient for the current objective. There is no Veldwake-specific shape grammar yet — one family, three silhouette classes, one palette, terrain-scale voxels, and rules written to make a landmark read as deliberate rather than to make it read as *this world's* architecture. | Recorded as a product and art-direction limitation, in the same sense as KI-025 and KI-028, and deliberately not answered inside M8: no props, decoration, interiors, second family, second material system, particles, banners or lights were added in response. It is evidence for a later decision about architectural art direction, a stronger shape grammar, and a procedural built-world identity — the kind of milestone that would start from [`audiovisual/LANDMARK_STYLE.md`](audiovisual/LANDMARK_STYLE.md) and ask what makes a Veldwake ruin unlike anyone else's. |

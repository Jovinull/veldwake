//! Minimal dependency-free crate for validating Veldwake's Rust foundation.
//!
//! Shared primitives will be added only when a concrete subsystem needs them. This crate is not a
//! dumping ground for unrelated helpers or a promise that every subsystem must depend on it.

#![forbid(unsafe_code)]
